workflows:
  ios-workflow:
    name: Ayna iOS Build
    max_build_duration: 60
    integrations:
      app_store_connect: Codemagic
    environment:
      xcode: latest
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.erenzeyb.ayna
      vars:
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
    scripts:
      - name: Install npm dependencies
        script: npm install
      - name: Install Capacitor CLI
        script: npm install @capacitor/cli @capacitor/core @capacitor/ios
      - name: Sync Capacitor
        script: npx cap sync ios
      - name: Patch Info.plist
        script: |
          PLIST="ios/App/App/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :CFBundleIconName string AppIcon" "$PLIST" || \
          /usr/libexec/PlistBuddy -c "Set :CFBundleIconName AppIcon" "$PLIST"
          echo "CFBundleIconName set"
      - name: Set build number
        script: |
          cd ios/App
          LATEST=$(app-store-connect get-latest-build-number 6780380139 || echo "1")
          NEW=$((LATEST + 1))
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $NEW" App/Info.plist
          echo "Build number set to $NEW"
      - name: Install CocoaPods
        script: |
          cd ios/App
          pod install
      - name: Build iOS
        script: |
          xcode-project use-profiles
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: false
